{
  "name": "dialogflow-nexmo",
  "licence": "Apache-2.0",
  "description": "A bridge between the Nexmo Messaging API and Google Dialogflow.",
  "main": "src/dialogflow-nexmo.js",
  "version": "1.0.1",
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage",
    "lint": "yarn run lint:eslint",
    "lint:eslint": "eslint ./src/**/*.js",
    "format": "yarn run lint --fix"
  },
  "engines": {
    "node": ">=10.4.1"
  },
  "dependencies": {
    "dialogflow": "^0.7.0",
    "lodash": "^4.17.11",
    "nexmo": "^2.4.0"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.0",
    "eslint": "^5.10.0",
    "eslint-config-prettier": "^3.3.0",
    "eslint-plugin-prettier": "^3.0.0",
    "jest": "^23.6.0",
    "prettier": "^1.15.2"
  }
}
